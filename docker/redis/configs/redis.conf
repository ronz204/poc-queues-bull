# Append-only persistence so queue/lock/cache state survives a container restart.
appendonly yes
appendfsync everysec

# BullMQ stores job state as Redis keys it expects to still be there — evicting
# under memory pressure would silently corrupt queue state, so refuse instead.
maxmemory-policy noeviction
